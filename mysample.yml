---
- hosts: all
  become: yes
  tasks:
           - name: i wnat to install tree
             apt:
                     name: tree
                     state: present

           - name: i wnat to install wget
             apt:
                     name: wget
                     state: present

